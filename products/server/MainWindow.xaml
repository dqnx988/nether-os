<Window x:Class="ServerApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NetherOS Server" Height="500" Width="520" MinWidth="450" MinHeight="420"
        Background="#121212" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#2a2a2a"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3a3a3a"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#00b894"/>
                                <Setter Property="Foreground" Value="#121212"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#555555"/>
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#2a2a2a"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,12"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="CaretBrush" Value="#00b894"/>
        </Style>
        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#00b894"/>
            <Setter Property="Foreground" Value="#121212"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="ProtocolCard" TargetType="Border">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="BorderBrush" Value="#3F3F3F"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2D2D2D"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ProtocolCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="PortTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#1b1b1b"/>
            <Setter Property="Foreground" Value="#e0e0e0"/>
            <Setter Property="BorderBrush" Value="#3F3F3F"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="CaretBrush" Value="#00b894"/>
            <Setter Property="MaxLength" Value="5"/>
            <Setter Property="Width" Value="70"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsFocused" Value="True">
                    <Setter Property="BorderBrush" Value="#00b894"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="PortLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#757575"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>
        <Style x:Key="StatusText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#00b894"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style x:Key="ErrorText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#e74c3c"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>
    </Window.Resources>
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="Directory" Foreground="#757575" FontSize="12" FontWeight="Medium" Margin="0,0,0,10"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Name="DirectoryPath" Style="{StaticResource ModernTextBox}"/>
            <Button Grid.Column="1" Content="Browse" Style="{StaticResource ModernButton}" Click="Browse_Click" Margin="12,0,0,0"/>
        </Grid>

        <TextBlock Grid.Row="3" Text="Protocols" Foreground="#757575" FontSize="12" FontWeight="Medium" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="4">
            <Border Name="HttpCard" Style="{StaticResource ProtocolCard}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                        <CheckBox Name="HttpEnabled" IsChecked="True" VerticalAlignment="Center" Style="{StaticResource ProtocolCheckBox}">
                        <CheckBox.LayoutTransform>
                            <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                        </CheckBox.LayoutTransform>
                        </CheckBox>
                        <TextBlock Text="HTTP" Foreground="White" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <TextBlock Text="Port" Style="{StaticResource PortLabel}"/>
                        <TextBox Name="HttpPortInput"
                                 Text="80"
                                 Style="{StaticResource PortTextBox}"
                                 TextChanged="Port_TextChanged"
                                 PreviewTextInput="PortInput_PreviewTextInput"
                                 DataObject.Pasting="PortInput_Pasting"/>
                    </StackPanel>
                </Grid>
            </Border>
            <TextBlock Name="HttpError" Style="{StaticResource ErrorText}" Visibility="Collapsed"/>

            <Border Name="FtpCard" Style="{StaticResource ProtocolCard}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                        <CheckBox Name="FtpEnabled" IsChecked="False" VerticalAlignment="Center" Style="{StaticResource ProtocolCheckBox}">
                        <CheckBox.LayoutTransform>
                            <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                        </CheckBox.LayoutTransform>
                        </CheckBox>
                        <TextBlock Text="FTP" Foreground="White" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <TextBlock Text="Port" Style="{StaticResource PortLabel}"/>
                        <TextBox Name="FtpPortInput"
                                 Text="21"
                                 Style="{StaticResource PortTextBox}"
                                 TextChanged="Port_TextChanged"
                                 PreviewTextInput="PortInput_PreviewTextInput"
                                 DataObject.Pasting="PortInput_Pasting"/>
                    </StackPanel>
                </Grid>
            </Border>
            <TextBlock Name="FtpError" Style="{StaticResource ErrorText}" Visibility="Collapsed"/>

            <Border Name="SftpCard" Style="{StaticResource ProtocolCard}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
                        <CheckBox Name="SftpEnabled" IsChecked="False" VerticalAlignment="Center" Style="{StaticResource ProtocolCheckBox}">
                        <CheckBox.LayoutTransform>
                            <ScaleTransform ScaleX="1.2" ScaleY="1.2"/>
                        </CheckBox.LayoutTransform>
                        </CheckBox>
                        <TextBlock Text="SFTP" Foreground="White" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <TextBlock Text="Port" Style="{StaticResource PortLabel}"/>
                        <TextBox Name="SftpPortInput"
                                 Text="22"
                                 Style="{StaticResource PortTextBox}"
                                 TextChanged="Port_TextChanged"
                                 PreviewTextInput="PortInput_PreviewTextInput"
                                 DataObject.Pasting="PortInput_Pasting"/>
                    </StackPanel>
                </Grid>
            </Border>
            <TextBlock Name="SftpError" Style="{StaticResource ErrorText}" Visibility="Collapsed"/>
        </StackPanel>

        <TextBlock Grid.Row="5" Name="StatusText" Style="{StaticResource StatusText}" Margin="0,12,0,0"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,24,0,0">
            <Button Name="LaunchBtn" Content="Launch Server" Style="{StaticResource AccentButton}" Click="Launch_Click"/>
            <Button Name="StopBtn" Content="Stop" Style="{StaticResource ModernButton}" Click="Stop_Click" Margin="12,0,0,0" IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>