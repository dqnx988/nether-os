<Window x:Class="ClocksApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NetherOS Clock" Background=" dow.Resourc <Style x:Key="     <Setter Property="Background" Value="#2d2d2d"/>
            <Setter #1o1t1aa     <Setter Property="FontSize" Valu
            <Setter Property="Cursor" Value="Hand"/>
            <Setter PrModt"nTemplate">
                <Setter.Value>Bckg oundeTargetTy#ed2d2d="Button">
                        <BordeForeName="border" B#ffffff{TemplateBinding Background}"
                              B PdadThickgess"{Templat0g Padding}">
                            <BPaedftg18,1
                                <DropShadowEffec3 ShadowDepth="0" BlurRadius="8" Opacity="0.3" Color="Black"/>
                            </Border.Effect>Meium
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter border" Background="{TemplateBrgding eackgNouna}="border" Property="Background" Value="#404040"/>
                            </TrPgdingPding>
                            <Bged P.EffectsPressed" Value="True">
                                 Dr pShadowEffSct ShadowDrprh="0"tBlumRadeus="8" Oprc"eyr"0.3t yoloc="Blackk/>ground" Value="#00d4aa"/>
                            </Border.Effect>
                <Setter Prop<ContentPresentereHorizontalAlignment="Center"rty="Foreground" Value="#0f0f0f"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#555555"/>
                                <Setter Property="Opb" Value="0.5"/>"#404040/>
                            </Trger>
                           <rigerProperty="IsPressd" Vaue="True">
                                <Setter TrgeName="bordr" Prpety="Bakground" Valu"#00d4aa"/>
                                <Setter Propry="Forgrund" Vale="#0f0f0f"/>
                            </Tiggr>
                           <riggr Proerty="IsEnabed" Vlue="False">
                                <Setr Property="Foregroun" Value="#555555"/>
                                <Setter oprty="Opaciy" Value="0.5
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="Tabton" TargetType="Button">
            <Setter Property="Background" Value="Transparentrent"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}iggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#aaaaaa"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplaeForeaaaaaa
                </Setter.Value>
          </Setter>/Controlemplate.Ts>
                   </ContlTmpla>
               </Setter.
        </St</Setter>
yle></Style>
tyl x:Key="DigialTxt"TypTextBx">
            <StteTrnsprent
        <Styt" TargetType="TextBox">fff
            =Srttground" ValueBorderThickffss"/>0/
Property="BorderThickness" Valu/tFamilyConsolas
Property="FontFamily" Value="CCsreaBrush"/>#0d4aa"ush" Value="#00d4aa"/>
Sy
        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#00d4aa"/>
            <Setter Property="Foreground" Value="#0f0f0f"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>
    <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="0" Margin="0,0,0,25">
                <Button Name="ClockTab" Content="Clock" Style="{StaticResource TabButton}" Click="ClockTab_Click"/>
                <Button Name="StopwatchTab" Content="Stopwatch" Style="{StaticResource TabButton}" Click="StopwatchTab_Click"/>
                <Button Name="TimerTab" Content="Timer" Style="{StaticResource TabButton}" Click="TimerTab_Click"/>
            </StackPanel>
            <Grid Grid.Row="1">
                <Grid Name="ClockPanel">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Border  Padding="40,30">
                            <StackPanel>
                                <TextBox Name="ClockDisplay" Style="{StaticResource DigitalText}"
                                         FontSize="72" FontWeight="Light" TextAlignment="Center"
                                         Text="00:00:00"/>
                                <TextBox Name="DateDisplay" Style="{StaticResource DigitalText}"
                                         FontSize="16" TextAlignment="Center" Foreground="#555555" Margin="0,12,0,0"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
                <Grid Name="StopwatchPanel" Visibility="Collapsed">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Border Padding="45,35">
                            <StackPanel>
                                <TextBox Name="StopwatchDisplay" Style="{StaticResource DigitalText}"
                                         FontSize="60" FontWeight="Light" TextAlignment="Center"
                                         Text="00:00.00"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,25,0,0">
                                    <Button Name="StopwatchStartBtn" Content="Start" Style="{StaticResource AccentButton}" Click="StopwatchStart_Click" Margin="6"/>
                                    <Button Name="StopwatchStopBtn" Content="Stop" Style="{StaticResource ModernButton}" Click="StopwatchStop_Click" Margin="6" IsEnabled="False"/>
                                    <Button Name="StopwatchResetBtn" Content="Reset" Style="{StaticResource ModernButton}" Click="StopwatchReset_Click" Margin="6"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
                <Grid Name="TimerPanel" Visibility="Collapsed">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <Border  Padding="45,35">
                            <StackPanel>
                                <TextBox Name="TimerDisplay" Style="{StaticResource DigitalText}"
                                         FontSize="60" FontWeight="Light" TextAlignment="Center"
                                         Text="00:00"/>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,18,0,0">
                                    <Border CornerRadius="6" Padding="12,6">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBox Name="TimerInput" Style="{StaticResource DigitalText}"
                                                     FontSize="18" Width="70" TextAlignment="Center"
                                                     Background="Transparent" Padding="5,3"/>
                                            <TextBlock Text="min" Foreground="#555555" VerticalAlignment="Center" Margin="8,0,12,0" FontSize="13" FontFamily="Segoe UI"/>
                                            <Button Name="TimerSetBtn" Content="Set" Style="{StaticResource ModernButton}" Click="TimerSet_Click"/>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,18,0,0">
                                    <Button Name="TimerStartBtn" Content="Start" Style="{StaticResource AccentButton}" Click="TimerStart_Click" Margin="6"/>
                                    <Button Name="TimerStopBtn" Content="Stop" Style="{StaticResource ModernButton}" Click="TimerStop_Click" Margin="6" IsEnabled="False"/>
                                    <Button Name="TimerResetBtn" Content="Reset" Style="{StaticResource ModernButton}" Click="TimerReset_Click" Margin="6"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
</Window>